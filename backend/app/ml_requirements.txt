# ML Models Requirements for AGRI AI
# Install these packages for full AI functionality

# Core ML frameworks (already installed)
torch>=1.13.0
transformers>=4.21.0
numpy>=1.21.0
scipy>=1.9.0

# Speech-to-Text (Whisper) - already installed
openai-whisper>=20230314
librosa>=0.9.2
soundfile>=0.12.1

# Language Detection (FastText) - compatible version
# fasttext>=0.9.2  # Skip for now due to compilation issues

# Text-to-Speech (Coqui TTS)
TTS>=0.13.0    # Coqui TTS library

# Audio processing
# pyaudio>=0.2.11  # Skip for now (requires system dependencies)
pydub>=0.25.1

# Machine Translation (IndicTrans)
sentencepiece>=0.1.97
sacremoses>=0.0.53

# LLM Integration (Ollama)
requests>=2.28.0
ollama>=0.1.7

# Utility packages
tqdm>=4.64.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Additional useful packages
datasets>=2.0.0
accelerate>=0.20.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.20.0

# Note: Some packages may require additional system dependencies:
# 
# For audio processing:
# - Linux: sudo apt-get install portaudio19-dev python3-pyaudio
# - macOS: brew install portaudio
# - Windows: Usually works with pip install
#
# For Coqui TTS:
# - May require espeak-ng for some features
# - Linux: sudo apt-get install espeak espeak-data libespeak1 libespeak-dev
#
# For GPU support:
# - CUDA toolkit and drivers
# - torch with CUDA support: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Installation command:
# pip install -r ml_requirements.txt
#
# For Ollama:
# 1. Install Ollama: https://ollama.ai/download
# 2. Pull models: ollama pull llama2:7b
# 3. Start server: ollama serve
